const TwoD = () => {
    return ( <div style={{display:"flex",alignItems:"flex-start"}}>
        <p style={{color: "yellow",fontSize:"3rem"}}>W: {window.innerWidth}</p>
        <p style={{color: "yellow",fontSize:"3rem"}}>H: {window.innerHeight}</p>
    </div> );
}
 
export default TwoD;